/**
 * Prints a given string to the standard output (console).
 * @param {string} message - The string to be printed to STDOUT.
 */
function displayMessage(message) {
  console.log(message);
}

module.exports = displayMessage;
